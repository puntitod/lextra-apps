<div class="mb-6 text-center">
    @if (setting('logo'))
    <img src="{{ asset('storage/' . setting('logo')) }}" alt="Logo"
        style="height: 60px; max-width: 80px; width: auto; display: block; margin: 0 auto; margin-bottom: 0.5rem;">
    @endif
</div>
